version: '3'
services:
  db:
    image: mongo:latest
    networks: 
      - backend
    ports:
      - 27017:27017
  app: 
    image: colinaaa/hackweek:v0.1.2
    networks: 
      - backend
    ports:
      - 8080:8080
networks: 
  backend:
    driver: bridge